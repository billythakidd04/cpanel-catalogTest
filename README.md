run `./catapp` on the command line and follow instructions

Valid Commands:

* `./catapp`: runs the application
* `./catapp help`: displays this help message
* `./catapp add`: will ask some questions and attempt to add a new record
